# AGENTS.md

本文件只规定智能体修改代码时的决策、禁令和验收门禁。项目入口见 `README.md`，专题文档索引见 `docs/README.md`，启动见 `docs/getting-started.md`，目录与测试命令见 `docs/development.md`；完整配置见 `src/main/resources/application.yml`。

## 角色与输出

- 以 Java 资深开发、面向对象和领域驱动设计视角工作；封装细节、保持主流程清晰，组合优于继承。
- 思考与检索用英文，回复用中文；简短直接、言之有据，不确定时先核实代码。
- 新 Java 类必须包含 `@author alex` 和 `@since`。

## 架构边界

严格遵守领域驱动设计与六边形四层架构：

```text
interfaces/   控制器、请求/响应数据传输对象、边界校验与转换
app/          用例编排、事务；严禁业务规则
domain/       聚合、值对象、领域服务、写侧仓储；不依赖外层
infra/        数据库、文件、命令行工具、HTTP、缓存等外部适配
config/       Spring 装配与配置属性
```

控制器不处理业务规则；应用层只编排领域仓储、网关和领域服务；基础设施层不替领域层作业务判断。

### 业务逻辑归属判据

应用层出现以下代码，必须先把语义下沉到聚合根：

- 遍历聚合内部集合做条件查找。
- 对聚合取值方法的结果做非空、状态、角色等语义判断。
- 替聚合校验路径、必填、格式等构造期不变量；改为 `Aggregate.create(...)`。
- 用多个取值方法重组状态迁移、合法性校验或语义查询。

无不变量时聚合可以是数据载体；一旦出现业务语义，必须由聚合方法表达。聚合根不得注入或调用仓储；跨聚合以标识或参数引用，需要仓储编排时使用领域服务。

### 仓储与读模型

- 写侧仓储接口放在领域层，只负责聚合生命周期（通常 3～5 个方法），签名只使用领域类型；基础设施层负责实现。
- 纯查询使用 `XxxQueryService`，接口放在应用层或领域层，实现放在基础设施层；返回数据传输对象、视图对象或 `Map`，不得返回不完整聚合。
- ORM、JDBC、MyBatis、Spring Data 类型和注解不得泄漏到领域接口；SQL 和映射器不承载业务判断。
- 仓储与查询服务混在同一接口时必须拆分。

## 测试驱动开发门禁（强制）

修改 Java 文件前按顺序判断：

1. 是否涉及 `if`、`switch`、条件分支或业务判断？
2. 否：纯委托、固定生命周期、配置、文档可直接修改。
3. 是：先按上述判据确认归属层；错位的规则先下沉领域层，不能给错位代码补测试。
4. 归属正确后，先写/改测试见红，再做最小实现见绿，最后重构保绿。

测试使用 JUnit 5 和 Mockito，采用“给定—当—则”结构；非 `live` 测试不得调用共享或外部数据库、Redis、消息队列、HTTP 服务或真实命令行工具，任务范围内的临时 SQLite 或文件系统集成测试除外。按层选择：

| 层 | 隔离方式 | 重点 |
| --- | --- | --- |
| 领域层 | 不模拟领域对象，不启动 Spring | 不变量、状态迁移、业务规则 |
| 应用层 | 模拟仓储、网关和领域服务 | 编排顺序、事务边界 |
| 基础设施层 | 模拟客户端和映射器，或使用 SQLite、`@TempDir` 轻量集成 | 缓存、协议和持久化适配 |
| 接口层 | 模拟应用服务；必要时使用 `@WebMvcTest` | 参数、数据传输对象、状态码 |
| 映射器、SQL、JPA 实体 | 集成测试 | 查询与映射正确性 |

完整 `@SpringBootTest` 仅用于服务器推送事件的时序与续传、事务代理、调度器、过滤器链、会话持久化等跨切边界。项目级测试驱动开发约定使用 `/java-tdd`，快速测试使用 `/fast-test`。

## 开发约定

- 修改不熟悉的子系统前，先从 `docs/README.md` 找到对应专题并完整阅读；业务规则以领域模型、接口授权和测试为准，不在本文件重复定义。
- 新增辅助类、服务、适配器、校验器、解析器、仓储或抽象前，先用 `rg` 搜索现有能力。
- 改动保持在用户请求范围内；除非安全完成任务所必需，不做大范围整理。
- 样板代码优先 Lombok/MapStruct；通用能力优先 Guava、Commons、Jackson、Spring Util，禁止重复造轮子。
- 禁止通配符导入；保持项目声明的 Java 21，不得降级 `java.version` 规避环境问题。
- 不主动修改 `agent-paths.yml`、`env.local`、`data/`、本地数据库或生成产物，除非任务明确要求。
- 前端源码只放 `frontend/`；可复用纯逻辑先抽到 `frontend/js/lib`，再在 `tests/unit` 补 Vitest。禁止在已停用的 `src/main/resources/static` 新增页面。
- Playwright 优先使用语义化定位器或稳定的 `data-test`，不得依赖 Element Plus 内部类名。

## 敏感信息

- 禁止在源码、配置模板、文档、日志、测试夹具或制品中新增硬编码凭据；生产环境优先使用进程环境或密钥存储。
- 必须文件化的服务端秘密只放 Git 忽略的 `data/secrets.properties`；写入前执行 `git check-ignore -q data/<path>`。目录权限 `700`，文件权限 `600`。
- 不得读取、复制或改写用户级 `~/.codex`、`~/.claude` 认证目录；除非用户明确改变命令行工具鉴权方式，不把命令行工具接口密钥写入 `data/secrets.properties`。
- 命令输出和回复不得打印敏感值，只能确认变量名、是否存在或不可逆摘要；禁止 `git add -f data/...`。
- `env.local` 仅保留历史防误提交规则，不作为新增秘密的存储位置。

## 构建与测试

- 完整命令见 `docs/development.md#测试`；优先运行覆盖改动的最小测试。
- 命令行工具测试必须使用 `TestCliStub`、运行时替身或端到端测试夹具；只有明确标记的 `live` 测试可以调用真实命令行工具或登录态。
- Linux 直接运行 Maven 时使用 `JAVA_HOME=/usr/local/jdk-21`。
- 显式运行默认排除的慢分组时必须覆盖 `test.excludedGroups`，具体分组以 `pom.xml` 为准。
- 不主动执行 `git commit`、`git push`、`mvn package`、`./scripts/service.sh restart`、部署或生产写操作，除非用户明确要求相应操作。
- 向 `master` 推送前必须完成 `docs/development.md#发布前门禁` 所列的后端、`frontend/`、`tests/` 三组持续集成门禁；后两个工程的 `typecheck` 都必须运行。

## 完成前检查

- Java 业务逻辑：有实现前能够失败的测试，并完成红—绿—重构。
- 控制器和接口：覆盖参数校验、状态码与安全响应投影。
- SQLite 仓储：使用真实 SQLite 验证；文件存储使用 `@TempDir`。
- 前端行为：运行对应的 Vitest 或 Playwright 用例。
- 涉及授权、并发、幂等、路径或外部副作用时，按影响覆盖越权拒绝、冲突、重试、符号链接、`NOFOLLOW`、哈希、存活期限和清理。
- 替身、模拟或静态检查通过只证明对应自动化边界，不能替代明确要求的真实外部依赖验收。
- 无法运行测试时，明确说明原因、未验证项和残余风险。
