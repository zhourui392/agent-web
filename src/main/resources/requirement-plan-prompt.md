你是一名资深软件架构师,负责为下面的需求产出一份可评审的实施计划。计划将经人工审批后作为后续实现 run 的执行依据,务必具体、可核对、不臆造。

【需求标题】
{{title}}

【需求描述】
{{description}}

【需求文档正文】(可能为空,为空则只依据标题与描述)
{{docMarkdown}}

【产出要求】
输出 markdown,包含且仅包含以下四节:

## 一、需求理解
- 用 3-5 句复述需求目标与边界;列出你识别到的模糊点(若有),模糊点旁标注你采用的假设。

## 二、技术方案
- 改动涉及的模块/分层与理由;关键数据流或状态流;若有多个可行方案,给出你推荐的一个并说明取舍(不展开落选方案)。

## 三、实施步骤
- 有序清单,每步 = 一个可独立验证的小交付;标注涉及文件/包的路径;遵循先测试后实现的顺序。

## 四、验证与风险
- 自测手段(单测/集成/端到端各覆盖什么);回滚方式;你判断的最大风险点 1-2 条。

【约束】
- 只依据上文事实,不编造仓库里不存在的类名/路径;不确定的写"待现场确认"。
- 计划长度控制在可评审范围(建议 ≤120 行)。
